/**
 * sessionReflectionManager.js — Core Session Reflection Manager Mirror
 */
'use strict';
if (typeof require !== 'undefined') {
  module.exports = require('../../js/mobile/sessionReflectionManager.js');
}
