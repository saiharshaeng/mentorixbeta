/**
 * applicationStateManager.js — Core Application State Manager Mirror
 */
'use strict';
if (typeof require !== 'undefined') {
  module.exports = require('../../js/state/applicationStateManager.js');
}
