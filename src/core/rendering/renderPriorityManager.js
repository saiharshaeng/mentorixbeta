/**
 * renderPriorityManager.js — Core Render Priority Manager Mirror
 */
'use strict';
if (typeof require !== 'undefined') {
  module.exports = require('../../js/rendering/renderPriorityManager.js');
}
